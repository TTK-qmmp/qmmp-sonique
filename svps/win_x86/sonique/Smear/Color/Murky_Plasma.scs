// Smear Color Script
// Author:	mykel@earthling.net

(declare init)

(declare color
 (if (> $value .8)
  (block (set $t (/ (- $value .8) .2))
	 (set $r $t)
  	 (set $b $t)
	 (set $g 1)
  )
  (if (> $value .5)
   (block (set $t ( / (- $value .5) .6))
          (set $r (- .5 $t))
	  (set $b 0)
	  (set $g (+ .5 $t))
   )
   (block (set $r $value)
	  (set $b 0)
	  (set $g $value)
   )
  )
 )
)
